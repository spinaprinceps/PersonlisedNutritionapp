buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {

        classpath 'com.android.tools.build:gradle:8.2.0' // ✅ Updated from 7.0.4
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
